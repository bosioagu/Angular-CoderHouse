import { NameLastnamePipe } from './name-lastname.pipe';

describe('NameLastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new NameLastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
